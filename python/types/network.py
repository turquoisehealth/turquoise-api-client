# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .entity_ref import EntityRef
from .network_object import NetworkObject
from .search_context import SearchContext


class Network(UniversalBaseModel):
    object: typing.Optional[NetworkObject] = None
    id: str = pydantic.Field()
    """
    Network identifier. Upstream-derived from the dataset and may change as the dataset is rebuilt. Re-resolve via search rather than persisting long-term.
    """

    name: str = pydantic.Field()
    """
    Display name of the network.
    """

    payer: EntityRef = pydantic.Field()
    """
    The network's payer as an {id, name} stub; the full entity lives at /v3/payers/{id}.
    """

    context: typing.Optional[SearchContext] = pydantic.Field(default=None)
    """
    Query-relative metadata (search only).
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
