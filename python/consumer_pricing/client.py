# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.care_nav_sort_type import CareNavSortType
from ..types.envelope_network import EnvelopeNetwork
from ..types.envelope_provider import EnvelopeProvider
from ..types.envelope_rate import EnvelopeRate
from ..types.envelope_ssp import EnvelopeSsp
from ..types.insurance_network_page import InsuranceNetworkPage
from ..types.location_input import LocationInput
from ..types.price_filter import PriceFilter
from ..types.provider_page import ProviderPage
from ..types.rate_compare_location import RateCompareLocation
from ..types.rate_comparison import RateComparison
from ..types.service_package_page import ServicePackagePage
from ..types.single_resource_envelope_network import SingleResourceEnvelopeNetwork
from ..types.single_resource_envelope_provider import SingleResourceEnvelopeProvider
from ..types.single_resource_envelope_rate_breakdown import SingleResourceEnvelopeRateBreakdown
from ..types.single_resource_envelope_ssp import SingleResourceEnvelopeSsp
from ..types.ssp_price_page import SspPricePage
from .raw_client import AsyncRawConsumerPricingClient, RawConsumerPricingClient
from .types.compare_ssp_prices_response import CompareSspPricesResponse
from .types.get_provider_ssp_prices_response import GetProviderSspPricesResponse
from .types.prices_request_rate_type import PricesRequestRateType
from .types.rate_compare_request_rate_type import RateCompareRequestRateType

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class ConsumerPricingClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawConsumerPricingClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawConsumerPricingClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawConsumerPricingClient
        """
        return self._raw_client

    def get_ssps(
        self,
        *,
        ssp_name: typing.Optional[str] = None,
        ssp_description: typing.Optional[str] = None,
        search: typing.Optional[str] = None,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ServicePackagePage:
        """
        Discover available service/surgery packages (SSPs). Supports optional name filtering and pagination.

        Parameters
        ----------
        ssp_name : typing.Optional[str]
            Filter SSPs by partial name match

        ssp_description : typing.Optional[str]
            Filter SSPs by partial patient description match

        search : typing.Optional[str]
            Search SSPs by name or patient description

        page : typing.Optional[int]
            Page number

        page_size : typing.Optional[int]
            Page size

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ServicePackagePage
            Successful Response

        Examples
        --------
        from turquoise-health import TurquoiseHealth

        client = TurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        client.consumer_pricing.get_ssps()
        """
        _response = self._raw_client.get_ssps(
            ssp_name=ssp_name,
            ssp_description=ssp_description,
            search=search,
            page=page,
            page_size=page_size,
            request_options=request_options,
        )
        return _response.data

    def get_insurance_networks(
        self,
        *,
        ssp_id: typing.Optional[str] = None,
        payer_name: typing.Optional[str] = None,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> InsuranceNetworkPage:
        """
        Discover insurance networks, optionally filtered by SSP or payer name.

        Parameters
        ----------
        ssp_id : typing.Optional[str]
            Limit networks to a specific SSP

        payer_name : typing.Optional[str]
            Filter by payer name

        page : typing.Optional[int]
            Page number

        page_size : typing.Optional[int]
            Page size

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        InsuranceNetworkPage
            Successful Response

        Examples
        --------
        from turquoise-health import TurquoiseHealth

        client = TurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        client.consumer_pricing.get_insurance_networks()
        """
        _response = self._raw_client.get_insurance_networks(
            ssp_id=ssp_id, payer_name=payer_name, page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def get_providers(
        self,
        *,
        provider_name: typing.Optional[str] = None,
        npi: typing.Optional[str] = None,
        city: typing.Optional[str] = None,
        state: typing.Optional[str] = None,
        zip_code: typing.Optional[str] = None,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ProviderPage:
        """
        Search for providers by name, NPI, or location.

        Parameters
        ----------
        provider_name : typing.Optional[str]
            Match provider name (case-insensitive)

        npi : typing.Optional[str]
            Exact provider NPI

        city : typing.Optional[str]
            Exact provider city

        state : typing.Optional[str]
            Two-letter state abbreviation

        zip_code : typing.Optional[str]
            Exact ZIP code

        page : typing.Optional[int]
            Page number

        page_size : typing.Optional[int]
            Page size

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ProviderPage
            Successful Response

        Examples
        --------
        from turquoise-health import TurquoiseHealth

        client = TurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        client.consumer_pricing.get_providers()
        """
        _response = self._raw_client.get_providers(
            provider_name=provider_name,
            npi=npi,
            city=city,
            state=state,
            zip_code=zip_code,
            page=page,
            page_size=page_size,
            request_options=request_options,
        )
        return _response.data

    def get_ssp_prices(
        self,
        ssp_id: str,
        *,
        location: LocationInput,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        network_id: typing.Optional[str] = OMIT,
        price_filter: typing.Optional[PriceFilter] = OMIT,
        npis: typing.Optional[typing.Sequence[str]] = OMIT,
        sort_by: typing.Optional[CareNavSortType] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SspPricePage:
        """
        List of providers that can satisfy a given SSP, with the total expected price, in a given geographic area. Requests support location filtering via ZIP code, CBSA, state, or coordinates. When no Network ID is provided, cash prices are shown.

        Parameters
        ----------
        ssp_id : str
            Standard service package identifier

        location : LocationInput
            Location to search for providers

        page : typing.Optional[int]
            Page number

        page_size : typing.Optional[int]
            Page size

        network_id : typing.Optional[str]
            Insurance network identifier

        price_filter : typing.Optional[PriceFilter]

        npis : typing.Optional[typing.Sequence[str]]

        sort_by : typing.Optional[CareNavSortType]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SspPricePage
            Successful Response

        Examples
        --------
        from turquoise-health import GeoSpace, LocationInput, TurquoiseHealth

        client = TurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        client.consumer_pricing.get_ssp_prices(ssp_id='DE000', location=LocationInput(geo_space=GeoSpace(zip_codes=['80129'], ), ), network_id='-7695283351826393948', sort_by="price-asc", )
        """
        _response = self._raw_client.get_ssp_prices(
            ssp_id,
            location=location,
            page=page,
            page_size=page_size,
            network_id=network_id,
            price_filter=price_filter,
            npis=npis,
            sort_by=sort_by,
            request_options=request_options,
        )
        return _response.data

    def get_provider_ssp_prices(
        self,
        ssp_id: str,
        provider_id: str,
        *,
        network_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GetProviderSspPricesResponse:
        """
        Get SSP breakdown and fee information about a selected SSP from a single provider. When no Network ID is provided, cash prices are shown.

        Parameters
        ----------
        ssp_id : str
            Standard service package identifier

        provider_id : str
            Provider identifier

        network_id : typing.Optional[str]
            Insurance network identifier

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetProviderSspPricesResponse
            Successful Response

        Examples
        --------
        from turquoise-health import TurquoiseHealth

        client = TurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        client.consumer_pricing.get_provider_ssp_prices(ssp_id='DE000', provider_id='provider_id', )
        """
        _response = self._raw_client.get_provider_ssp_prices(
            ssp_id, provider_id, network_id=network_id, request_options=request_options
        )
        return _response.data

    def compare_ssp_prices(
        self,
        ssp_id: str,
        *,
        location: LocationInput,
        network_id: typing.Optional[str] = OMIT,
        npis: typing.Optional[typing.Sequence[str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CompareSspPricesResponse:
        """
        Get summary statistics (min, max, average, quartiles) for prices matching the selected filters. When no Network ID is provided, cash prices are shown.

        Parameters
        ----------
        ssp_id : str
            Standard service package identifier

        location : LocationInput
            Location to search for providers

        network_id : typing.Optional[str]
            Insurance network identifier

        npis : typing.Optional[typing.Sequence[str]]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CompareSspPricesResponse
            Successful Response

        Examples
        --------
        from turquoise-health import GeoSpace, LocationInput, TurquoiseHealth

        client = TurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        client.consumer_pricing.compare_ssp_prices(ssp_id='DE000', location=LocationInput(geo_space=GeoSpace(state='CO', ), ), network_id='-7695283351826393948', )
        """
        _response = self._raw_client.compare_ssp_prices(
            ssp_id, location=location, network_id=network_id, npis=npis, request_options=request_options
        )
        return _response.data

    def v2list_ssps(
        self,
        *,
        name: typing.Optional[str] = None,
        description: typing.Optional[str] = None,
        search: typing.Optional[str] = None,
        page_size: typing.Optional[int] = None,
        cursor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EnvelopeSsp:
        """
        Discover available service/surgery packages (SSPs). Supports optional name filtering and pagination.

        Parameters
        ----------
        name : typing.Optional[str]
            Case-insensitive substring match on SSP name.

        description : typing.Optional[str]
            Case-insensitive substring match on SSP patient description.

        search : typing.Optional[str]
            Case-insensitive substring match across name OR patient description.

        page_size : typing.Optional[int]
            Page size.

        cursor : typing.Optional[str]
            Opaque cursor from a previous response's page.next_cursor.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EnvelopeSsp
            Successful Response

        Examples
        --------
        from turquoise-health import TurquoiseHealth

        client = TurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        client.consumer_pricing.v2list_ssps()
        """
        _response = self._raw_client.v2list_ssps(
            name=name,
            description=description,
            search=search,
            page_size=page_size,
            cursor=cursor,
            request_options=request_options,
        )
        return _response.data

    def v2get_ssp(
        self, ssp_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> SingleResourceEnvelopeSsp:
        """
        Retrieve an SSP name and patient-ready description from the relevant SSP ID.

        Parameters
        ----------
        ssp_id : str
            SSP identifier.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SingleResourceEnvelopeSsp
            Successful Response

        Examples
        --------
        from turquoise-health import TurquoiseHealth

        client = TurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        client.consumer_pricing.v2get_ssp(ssp_id='GA002', )
        """
        _response = self._raw_client.v2get_ssp(ssp_id, request_options=request_options)
        return _response.data

    def v2list_providers(
        self,
        *,
        name: typing.Optional[str] = None,
        npi: typing.Optional[str] = None,
        page_size: typing.Optional[int] = None,
        cursor: typing.Optional[str] = None,
        near_lat: typing.Optional[float] = None,
        near_lng: typing.Optional[float] = None,
        near_radius_m: typing.Optional[int] = None,
        within_state: typing.Optional[str] = None,
        within_cbsa_name: typing.Optional[str] = None,
        within_zip_codes: typing.Optional[typing.Sequence[str]] = None,
        zip_anchor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EnvelopeProvider:
        """
        Search for providers by name, NPI, or location.

        Parameters
        ----------
        name : typing.Optional[str]
            Case-insensitive substring match on provider name.

        npi : typing.Optional[str]
            Exact NPI match.

        page_size : typing.Optional[int]
            Page size.

        cursor : typing.Optional[str]
            Opaque cursor from a previous response's page.next_cursor.

        near_lat : typing.Optional[float]
            Latitude anchor for `near` mode. Must be paired with `near.lng`.

        near_lng : typing.Optional[float]
            Longitude anchor for `near` mode. Must be paired with `near.lat`.

        near_radius_m : typing.Optional[int]
            Search radius in meters for `near` mode. Defaults to 25 000 when omitted.

        within_state : typing.Optional[str]
            Two-letter USPS state code for `within` mode (exact match).

        within_cbsa_name : typing.Optional[str]
            CBSA name for `within` mode (case-insensitive substring match).

        within_zip_codes : typing.Optional[typing.Sequence[str]]
            ZIP codes for `within` mode (exact match, any-of). Repeat the param for multiple values.

        zip_anchor : typing.Optional[str]
            Convenience: resolves a ZIP to its centroid, then runs `near` with the default radius.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EnvelopeProvider
            Successful Response

        Examples
        --------
        from turquoise-health import TurquoiseHealth

        client = TurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        client.consumer_pricing.v2list_providers()
        """
        _response = self._raw_client.v2list_providers(
            name=name,
            npi=npi,
            page_size=page_size,
            cursor=cursor,
            near_lat=near_lat,
            near_lng=near_lng,
            near_radius_m=near_radius_m,
            within_state=within_state,
            within_cbsa_name=within_cbsa_name,
            within_zip_codes=within_zip_codes,
            zip_anchor=zip_anchor,
            request_options=request_options,
        )
        return _response.data

    def v2get_provider(
        self, provider_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> SingleResourceEnvelopeProvider:
        """
        Return relevant information about a specific provider from the provider ID, including name, NPI, and location.

        Parameters
        ----------
        provider_id : str
            Provider identifier.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SingleResourceEnvelopeProvider
            Successful Response

        Examples
        --------
        from turquoise-health import TurquoiseHealth

        client = TurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        client.consumer_pricing.v2get_provider(provider_id='21929', )
        """
        _response = self._raw_client.v2get_provider(provider_id, request_options=request_options)
        return _response.data

    def v2list_networks(
        self,
        *,
        name: typing.Optional[str] = None,
        payer_id: typing.Optional[str] = None,
        page_size: typing.Optional[int] = None,
        cursor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EnvelopeNetwork:
        """
        Discover insurance networks, optionally filtered by network name, payer name, or payer ID.

        Parameters
        ----------
        name : typing.Optional[str]
            Case-insensitive substring match on network or payer name.

        payer_id : typing.Optional[str]
            Filter to networks under this payer.

        page_size : typing.Optional[int]
            Page size.

        cursor : typing.Optional[str]
            Opaque cursor from a previous response's page.next_cursor.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EnvelopeNetwork
            Successful Response

        Examples
        --------
        from turquoise-health import TurquoiseHealth

        client = TurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        client.consumer_pricing.v2list_networks()
        """
        _response = self._raw_client.v2list_networks(
            name=name, payer_id=payer_id, page_size=page_size, cursor=cursor, request_options=request_options
        )
        return _response.data

    def v2get_network(
        self, network_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> SingleResourceEnvelopeNetwork:
        """
        Return relevant information about a specific insurance network from the network ID.

        Parameters
        ----------
        network_id : str
            Network identifier (Int64String on the wire).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SingleResourceEnvelopeNetwork
            Successful Response

        Examples
        --------
        from turquoise-health import TurquoiseHealth

        client = TurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        client.consumer_pricing.v2get_network(network_id='8361580493441765265', )
        """
        _response = self._raw_client.v2get_network(network_id, request_options=request_options)
        return _response.data

    def v2list_prices(
        self,
        *,
        ssp_id: typing.Optional[str] = OMIT,
        provider_id: typing.Optional[str] = OMIT,
        network_id: typing.Optional[str] = OMIT,
        rate_type: typing.Optional[PricesRequestRateType] = OMIT,
        location: typing.Optional[RateCompareLocation] = OMIT,
        page_size: typing.Optional[int] = OMIT,
        cursor: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EnvelopeRate:
        """
        List of providers that can satisfy a given SSP, with the total expected price, in a given geographic area. Requests support location filtering via ZIP code, CBSA name, state, or coordinates. When no Network ID is provided, cash prices are shown.

        Parameters
        ----------
        ssp_id : typing.Optional[str]
            Filter to a single SSP. Upstream-derived from the dataset and may change as the dataset is rebuilt. Do not bake into URLs, bookmarks, or persistent storage.

        provider_id : typing.Optional[str]
            Filter to a single provider. Upstream-derived from the dataset and may change as the dataset is rebuilt. Do not bake into URLs, bookmarks, or persistent storage.

        network_id : typing.Optional[str]
            Filter to a single insurance network. Int64String — JSON string-wrapped 64-bit integer, because JSON cannot safely represent values above 2^53. Upstream-derived from the dataset and may change as the dataset is rebuilt. Do not bake into URLs, bookmarks, or persistent storage.

        rate_type : typing.Optional[PricesRequestRateType]
            `cash` filters to rows with no network. `negotiated` requires a network match.

        location : typing.Optional[RateCompareLocation]
            Optional location scope. Exactly zero or one of `near`, `within`, or `zip_anchor` modes.

        page_size : typing.Optional[int]
            Page size, 1-250.

        cursor : typing.Optional[str]
            Opaque cursor from a previous response's `page.next_cursor`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EnvelopeRate
            Successful Response

        Examples
        --------
        from turquoise-health import RateCompareLocation, TurquoiseHealth

        client = TurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        client.consumer_pricing.v2list_prices(ssp_id='GA002', network_id='8361580493441765265', rate_type="negotiated", location=RateCompareLocation(zip_anchor='80202', ), )
        """
        _response = self._raw_client.v2list_prices(
            ssp_id=ssp_id,
            provider_id=provider_id,
            network_id=network_id,
            rate_type=rate_type,
            location=location,
            page_size=page_size,
            cursor=cursor,
            request_options=request_options,
        )
        return _response.data

    def v2get_price_breakdown(
        self,
        *,
        provider_id: str,
        ssp_id: str,
        network_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SingleResourceEnvelopeRateBreakdown:
        """
        Get SSP breakdown and fee information about a selected SSP from a single provider. When no Network ID is provided, cash prices are shown.

        Parameters
        ----------
        provider_id : str
            Provider identifier. Upstream-derived from the dataset and may change as the dataset is rebuilt. Do not bake into URLs, bookmarks, or persistent storage.

        ssp_id : str
            SSP identifier. Upstream-derived from the dataset and may change as the dataset is rebuilt. Do not bake into URLs, bookmarks, or persistent storage.

        network_id : typing.Optional[str]
            Optional network filter. Omit for cash-price breakdown. Int64String — JSON string-wrapped 64-bit integer, because JSON cannot safely represent values above 2^53. Upstream-derived from the dataset and may change as the dataset is rebuilt. Do not bake into URLs, bookmarks, or persistent storage.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SingleResourceEnvelopeRateBreakdown
            Successful Response

        Examples
        --------
        from turquoise-health import TurquoiseHealth

        client = TurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        client.consumer_pricing.v2get_price_breakdown(provider_id='21929', ssp_id='RA011', )
        """
        _response = self._raw_client.v2get_price_breakdown(
            provider_id=provider_id, ssp_id=ssp_id, network_id=network_id, request_options=request_options
        )
        return _response.data

    def v2compare_prices(
        self,
        *,
        ssp_id: typing.Optional[str] = OMIT,
        provider_id: typing.Optional[str] = OMIT,
        network_id: typing.Optional[str] = OMIT,
        rate_type: typing.Optional[RateCompareRequestRateType] = OMIT,
        location: typing.Optional[RateCompareLocation] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> RateComparison:
        """
        Get summary statistics (min, max, average, quartiles) for prices matching the selected filters. When no Network ID is provided, cash prices are shown.

        Parameters
        ----------
        ssp_id : typing.Optional[str]
            Filter to a single SSP. Upstream-derived from the dataset and may change as the dataset is rebuilt. Do not bake into URLs, bookmarks, or persistent storage.

        provider_id : typing.Optional[str]
            Filter to a single provider. Upstream-derived from the dataset and may change as the dataset is rebuilt. Do not bake into URLs, bookmarks, or persistent storage.

        network_id : typing.Optional[str]
            Filter to a single insurance network. Int64String — JSON string-wrapped 64-bit integer, because JSON cannot safely represent values above 2^53. Upstream-derived from the dataset and may change as the dataset is rebuilt. Do not bake into URLs, bookmarks, or persistent storage.

        rate_type : typing.Optional[RateCompareRequestRateType]
            `cash` filters to rows with no network. `negotiated` requires a network match.

        location : typing.Optional[RateCompareLocation]
            Optional location scope. Exactly zero or one of `near`, `within`, or `zip_anchor` modes.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RateComparison
            Successful Response

        Examples
        --------
        from turquoise-health import RateCompareLocation, TurquoiseHealth

        client = TurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        client.consumer_pricing.v2compare_prices(ssp_id='RA011', network_id='8361580493441765265', location=RateCompareLocation(within_state='CO', ), )
        """
        _response = self._raw_client.v2compare_prices(
            ssp_id=ssp_id,
            provider_id=provider_id,
            network_id=network_id,
            rate_type=rate_type,
            location=location,
            request_options=request_options,
        )
        return _response.data


class AsyncConsumerPricingClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawConsumerPricingClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawConsumerPricingClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawConsumerPricingClient
        """
        return self._raw_client

    async def get_ssps(
        self,
        *,
        ssp_name: typing.Optional[str] = None,
        ssp_description: typing.Optional[str] = None,
        search: typing.Optional[str] = None,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ServicePackagePage:
        """
        Discover available service/surgery packages (SSPs). Supports optional name filtering and pagination.

        Parameters
        ----------
        ssp_name : typing.Optional[str]
            Filter SSPs by partial name match

        ssp_description : typing.Optional[str]
            Filter SSPs by partial patient description match

        search : typing.Optional[str]
            Search SSPs by name or patient description

        page : typing.Optional[int]
            Page number

        page_size : typing.Optional[int]
            Page size

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ServicePackagePage
            Successful Response

        Examples
        --------
        import asyncio

        from turquoise-health import AsyncTurquoiseHealth

        client = AsyncTurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        async def main() -> None:
            await client.consumer_pricing.get_ssps()
        asyncio.run(main())
        """
        _response = await self._raw_client.get_ssps(
            ssp_name=ssp_name,
            ssp_description=ssp_description,
            search=search,
            page=page,
            page_size=page_size,
            request_options=request_options,
        )
        return _response.data

    async def get_insurance_networks(
        self,
        *,
        ssp_id: typing.Optional[str] = None,
        payer_name: typing.Optional[str] = None,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> InsuranceNetworkPage:
        """
        Discover insurance networks, optionally filtered by SSP or payer name.

        Parameters
        ----------
        ssp_id : typing.Optional[str]
            Limit networks to a specific SSP

        payer_name : typing.Optional[str]
            Filter by payer name

        page : typing.Optional[int]
            Page number

        page_size : typing.Optional[int]
            Page size

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        InsuranceNetworkPage
            Successful Response

        Examples
        --------
        import asyncio

        from turquoise-health import AsyncTurquoiseHealth

        client = AsyncTurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        async def main() -> None:
            await client.consumer_pricing.get_insurance_networks()
        asyncio.run(main())
        """
        _response = await self._raw_client.get_insurance_networks(
            ssp_id=ssp_id, payer_name=payer_name, page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def get_providers(
        self,
        *,
        provider_name: typing.Optional[str] = None,
        npi: typing.Optional[str] = None,
        city: typing.Optional[str] = None,
        state: typing.Optional[str] = None,
        zip_code: typing.Optional[str] = None,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ProviderPage:
        """
        Search for providers by name, NPI, or location.

        Parameters
        ----------
        provider_name : typing.Optional[str]
            Match provider name (case-insensitive)

        npi : typing.Optional[str]
            Exact provider NPI

        city : typing.Optional[str]
            Exact provider city

        state : typing.Optional[str]
            Two-letter state abbreviation

        zip_code : typing.Optional[str]
            Exact ZIP code

        page : typing.Optional[int]
            Page number

        page_size : typing.Optional[int]
            Page size

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ProviderPage
            Successful Response

        Examples
        --------
        import asyncio

        from turquoise-health import AsyncTurquoiseHealth

        client = AsyncTurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        async def main() -> None:
            await client.consumer_pricing.get_providers()
        asyncio.run(main())
        """
        _response = await self._raw_client.get_providers(
            provider_name=provider_name,
            npi=npi,
            city=city,
            state=state,
            zip_code=zip_code,
            page=page,
            page_size=page_size,
            request_options=request_options,
        )
        return _response.data

    async def get_ssp_prices(
        self,
        ssp_id: str,
        *,
        location: LocationInput,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        network_id: typing.Optional[str] = OMIT,
        price_filter: typing.Optional[PriceFilter] = OMIT,
        npis: typing.Optional[typing.Sequence[str]] = OMIT,
        sort_by: typing.Optional[CareNavSortType] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SspPricePage:
        """
        List of providers that can satisfy a given SSP, with the total expected price, in a given geographic area. Requests support location filtering via ZIP code, CBSA, state, or coordinates. When no Network ID is provided, cash prices are shown.

        Parameters
        ----------
        ssp_id : str
            Standard service package identifier

        location : LocationInput
            Location to search for providers

        page : typing.Optional[int]
            Page number

        page_size : typing.Optional[int]
            Page size

        network_id : typing.Optional[str]
            Insurance network identifier

        price_filter : typing.Optional[PriceFilter]

        npis : typing.Optional[typing.Sequence[str]]

        sort_by : typing.Optional[CareNavSortType]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SspPricePage
            Successful Response

        Examples
        --------
        import asyncio

        from turquoise-health import AsyncTurquoiseHealth, GeoSpace, LocationInput

        client = AsyncTurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        async def main() -> None:
            await client.consumer_pricing.get_ssp_prices(ssp_id='DE000', location=LocationInput(geo_space=GeoSpace(zip_codes=['80129'], ), ), network_id='-7695283351826393948', sort_by="price-asc", )
        asyncio.run(main())
        """
        _response = await self._raw_client.get_ssp_prices(
            ssp_id,
            location=location,
            page=page,
            page_size=page_size,
            network_id=network_id,
            price_filter=price_filter,
            npis=npis,
            sort_by=sort_by,
            request_options=request_options,
        )
        return _response.data

    async def get_provider_ssp_prices(
        self,
        ssp_id: str,
        provider_id: str,
        *,
        network_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GetProviderSspPricesResponse:
        """
        Get SSP breakdown and fee information about a selected SSP from a single provider. When no Network ID is provided, cash prices are shown.

        Parameters
        ----------
        ssp_id : str
            Standard service package identifier

        provider_id : str
            Provider identifier

        network_id : typing.Optional[str]
            Insurance network identifier

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetProviderSspPricesResponse
            Successful Response

        Examples
        --------
        import asyncio

        from turquoise-health import AsyncTurquoiseHealth

        client = AsyncTurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        async def main() -> None:
            await client.consumer_pricing.get_provider_ssp_prices(ssp_id='DE000', provider_id='provider_id', )
        asyncio.run(main())
        """
        _response = await self._raw_client.get_provider_ssp_prices(
            ssp_id, provider_id, network_id=network_id, request_options=request_options
        )
        return _response.data

    async def compare_ssp_prices(
        self,
        ssp_id: str,
        *,
        location: LocationInput,
        network_id: typing.Optional[str] = OMIT,
        npis: typing.Optional[typing.Sequence[str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CompareSspPricesResponse:
        """
        Get summary statistics (min, max, average, quartiles) for prices matching the selected filters. When no Network ID is provided, cash prices are shown.

        Parameters
        ----------
        ssp_id : str
            Standard service package identifier

        location : LocationInput
            Location to search for providers

        network_id : typing.Optional[str]
            Insurance network identifier

        npis : typing.Optional[typing.Sequence[str]]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CompareSspPricesResponse
            Successful Response

        Examples
        --------
        import asyncio

        from turquoise-health import AsyncTurquoiseHealth, GeoSpace, LocationInput

        client = AsyncTurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        async def main() -> None:
            await client.consumer_pricing.compare_ssp_prices(ssp_id='DE000', location=LocationInput(geo_space=GeoSpace(state='CO', ), ), network_id='-7695283351826393948', )
        asyncio.run(main())
        """
        _response = await self._raw_client.compare_ssp_prices(
            ssp_id, location=location, network_id=network_id, npis=npis, request_options=request_options
        )
        return _response.data

    async def v2list_ssps(
        self,
        *,
        name: typing.Optional[str] = None,
        description: typing.Optional[str] = None,
        search: typing.Optional[str] = None,
        page_size: typing.Optional[int] = None,
        cursor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EnvelopeSsp:
        """
        Discover available service/surgery packages (SSPs). Supports optional name filtering and pagination.

        Parameters
        ----------
        name : typing.Optional[str]
            Case-insensitive substring match on SSP name.

        description : typing.Optional[str]
            Case-insensitive substring match on SSP patient description.

        search : typing.Optional[str]
            Case-insensitive substring match across name OR patient description.

        page_size : typing.Optional[int]
            Page size.

        cursor : typing.Optional[str]
            Opaque cursor from a previous response's page.next_cursor.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EnvelopeSsp
            Successful Response

        Examples
        --------
        import asyncio

        from turquoise-health import AsyncTurquoiseHealth

        client = AsyncTurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        async def main() -> None:
            await client.consumer_pricing.v2list_ssps()
        asyncio.run(main())
        """
        _response = await self._raw_client.v2list_ssps(
            name=name,
            description=description,
            search=search,
            page_size=page_size,
            cursor=cursor,
            request_options=request_options,
        )
        return _response.data

    async def v2get_ssp(
        self, ssp_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> SingleResourceEnvelopeSsp:
        """
        Retrieve an SSP name and patient-ready description from the relevant SSP ID.

        Parameters
        ----------
        ssp_id : str
            SSP identifier.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SingleResourceEnvelopeSsp
            Successful Response

        Examples
        --------
        import asyncio

        from turquoise-health import AsyncTurquoiseHealth

        client = AsyncTurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        async def main() -> None:
            await client.consumer_pricing.v2get_ssp(ssp_id='GA002', )
        asyncio.run(main())
        """
        _response = await self._raw_client.v2get_ssp(ssp_id, request_options=request_options)
        return _response.data

    async def v2list_providers(
        self,
        *,
        name: typing.Optional[str] = None,
        npi: typing.Optional[str] = None,
        page_size: typing.Optional[int] = None,
        cursor: typing.Optional[str] = None,
        near_lat: typing.Optional[float] = None,
        near_lng: typing.Optional[float] = None,
        near_radius_m: typing.Optional[int] = None,
        within_state: typing.Optional[str] = None,
        within_cbsa_name: typing.Optional[str] = None,
        within_zip_codes: typing.Optional[typing.Sequence[str]] = None,
        zip_anchor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EnvelopeProvider:
        """
        Search for providers by name, NPI, or location.

        Parameters
        ----------
        name : typing.Optional[str]
            Case-insensitive substring match on provider name.

        npi : typing.Optional[str]
            Exact NPI match.

        page_size : typing.Optional[int]
            Page size.

        cursor : typing.Optional[str]
            Opaque cursor from a previous response's page.next_cursor.

        near_lat : typing.Optional[float]
            Latitude anchor for `near` mode. Must be paired with `near.lng`.

        near_lng : typing.Optional[float]
            Longitude anchor for `near` mode. Must be paired with `near.lat`.

        near_radius_m : typing.Optional[int]
            Search radius in meters for `near` mode. Defaults to 25 000 when omitted.

        within_state : typing.Optional[str]
            Two-letter USPS state code for `within` mode (exact match).

        within_cbsa_name : typing.Optional[str]
            CBSA name for `within` mode (case-insensitive substring match).

        within_zip_codes : typing.Optional[typing.Sequence[str]]
            ZIP codes for `within` mode (exact match, any-of). Repeat the param for multiple values.

        zip_anchor : typing.Optional[str]
            Convenience: resolves a ZIP to its centroid, then runs `near` with the default radius.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EnvelopeProvider
            Successful Response

        Examples
        --------
        import asyncio

        from turquoise-health import AsyncTurquoiseHealth

        client = AsyncTurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        async def main() -> None:
            await client.consumer_pricing.v2list_providers()
        asyncio.run(main())
        """
        _response = await self._raw_client.v2list_providers(
            name=name,
            npi=npi,
            page_size=page_size,
            cursor=cursor,
            near_lat=near_lat,
            near_lng=near_lng,
            near_radius_m=near_radius_m,
            within_state=within_state,
            within_cbsa_name=within_cbsa_name,
            within_zip_codes=within_zip_codes,
            zip_anchor=zip_anchor,
            request_options=request_options,
        )
        return _response.data

    async def v2get_provider(
        self, provider_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> SingleResourceEnvelopeProvider:
        """
        Return relevant information about a specific provider from the provider ID, including name, NPI, and location.

        Parameters
        ----------
        provider_id : str
            Provider identifier.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SingleResourceEnvelopeProvider
            Successful Response

        Examples
        --------
        import asyncio

        from turquoise-health import AsyncTurquoiseHealth

        client = AsyncTurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        async def main() -> None:
            await client.consumer_pricing.v2get_provider(provider_id='21929', )
        asyncio.run(main())
        """
        _response = await self._raw_client.v2get_provider(provider_id, request_options=request_options)
        return _response.data

    async def v2list_networks(
        self,
        *,
        name: typing.Optional[str] = None,
        payer_id: typing.Optional[str] = None,
        page_size: typing.Optional[int] = None,
        cursor: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EnvelopeNetwork:
        """
        Discover insurance networks, optionally filtered by network name, payer name, or payer ID.

        Parameters
        ----------
        name : typing.Optional[str]
            Case-insensitive substring match on network or payer name.

        payer_id : typing.Optional[str]
            Filter to networks under this payer.

        page_size : typing.Optional[int]
            Page size.

        cursor : typing.Optional[str]
            Opaque cursor from a previous response's page.next_cursor.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EnvelopeNetwork
            Successful Response

        Examples
        --------
        import asyncio

        from turquoise-health import AsyncTurquoiseHealth

        client = AsyncTurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        async def main() -> None:
            await client.consumer_pricing.v2list_networks()
        asyncio.run(main())
        """
        _response = await self._raw_client.v2list_networks(
            name=name, payer_id=payer_id, page_size=page_size, cursor=cursor, request_options=request_options
        )
        return _response.data

    async def v2get_network(
        self, network_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> SingleResourceEnvelopeNetwork:
        """
        Return relevant information about a specific insurance network from the network ID.

        Parameters
        ----------
        network_id : str
            Network identifier (Int64String on the wire).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SingleResourceEnvelopeNetwork
            Successful Response

        Examples
        --------
        import asyncio

        from turquoise-health import AsyncTurquoiseHealth

        client = AsyncTurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        async def main() -> None:
            await client.consumer_pricing.v2get_network(network_id='8361580493441765265', )
        asyncio.run(main())
        """
        _response = await self._raw_client.v2get_network(network_id, request_options=request_options)
        return _response.data

    async def v2list_prices(
        self,
        *,
        ssp_id: typing.Optional[str] = OMIT,
        provider_id: typing.Optional[str] = OMIT,
        network_id: typing.Optional[str] = OMIT,
        rate_type: typing.Optional[PricesRequestRateType] = OMIT,
        location: typing.Optional[RateCompareLocation] = OMIT,
        page_size: typing.Optional[int] = OMIT,
        cursor: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EnvelopeRate:
        """
        List of providers that can satisfy a given SSP, with the total expected price, in a given geographic area. Requests support location filtering via ZIP code, CBSA name, state, or coordinates. When no Network ID is provided, cash prices are shown.

        Parameters
        ----------
        ssp_id : typing.Optional[str]
            Filter to a single SSP. Upstream-derived from the dataset and may change as the dataset is rebuilt. Do not bake into URLs, bookmarks, or persistent storage.

        provider_id : typing.Optional[str]
            Filter to a single provider. Upstream-derived from the dataset and may change as the dataset is rebuilt. Do not bake into URLs, bookmarks, or persistent storage.

        network_id : typing.Optional[str]
            Filter to a single insurance network. Int64String — JSON string-wrapped 64-bit integer, because JSON cannot safely represent values above 2^53. Upstream-derived from the dataset and may change as the dataset is rebuilt. Do not bake into URLs, bookmarks, or persistent storage.

        rate_type : typing.Optional[PricesRequestRateType]
            `cash` filters to rows with no network. `negotiated` requires a network match.

        location : typing.Optional[RateCompareLocation]
            Optional location scope. Exactly zero or one of `near`, `within`, or `zip_anchor` modes.

        page_size : typing.Optional[int]
            Page size, 1-250.

        cursor : typing.Optional[str]
            Opaque cursor from a previous response's `page.next_cursor`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EnvelopeRate
            Successful Response

        Examples
        --------
        import asyncio

        from turquoise-health import AsyncTurquoiseHealth, RateCompareLocation

        client = AsyncTurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        async def main() -> None:
            await client.consumer_pricing.v2list_prices(ssp_id='GA002', network_id='8361580493441765265', rate_type="negotiated", location=RateCompareLocation(zip_anchor='80202', ), )
        asyncio.run(main())
        """
        _response = await self._raw_client.v2list_prices(
            ssp_id=ssp_id,
            provider_id=provider_id,
            network_id=network_id,
            rate_type=rate_type,
            location=location,
            page_size=page_size,
            cursor=cursor,
            request_options=request_options,
        )
        return _response.data

    async def v2get_price_breakdown(
        self,
        *,
        provider_id: str,
        ssp_id: str,
        network_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SingleResourceEnvelopeRateBreakdown:
        """
        Get SSP breakdown and fee information about a selected SSP from a single provider. When no Network ID is provided, cash prices are shown.

        Parameters
        ----------
        provider_id : str
            Provider identifier. Upstream-derived from the dataset and may change as the dataset is rebuilt. Do not bake into URLs, bookmarks, or persistent storage.

        ssp_id : str
            SSP identifier. Upstream-derived from the dataset and may change as the dataset is rebuilt. Do not bake into URLs, bookmarks, or persistent storage.

        network_id : typing.Optional[str]
            Optional network filter. Omit for cash-price breakdown. Int64String — JSON string-wrapped 64-bit integer, because JSON cannot safely represent values above 2^53. Upstream-derived from the dataset and may change as the dataset is rebuilt. Do not bake into URLs, bookmarks, or persistent storage.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SingleResourceEnvelopeRateBreakdown
            Successful Response

        Examples
        --------
        import asyncio

        from turquoise-health import AsyncTurquoiseHealth

        client = AsyncTurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        async def main() -> None:
            await client.consumer_pricing.v2get_price_breakdown(provider_id='21929', ssp_id='RA011', )
        asyncio.run(main())
        """
        _response = await self._raw_client.v2get_price_breakdown(
            provider_id=provider_id, ssp_id=ssp_id, network_id=network_id, request_options=request_options
        )
        return _response.data

    async def v2compare_prices(
        self,
        *,
        ssp_id: typing.Optional[str] = OMIT,
        provider_id: typing.Optional[str] = OMIT,
        network_id: typing.Optional[str] = OMIT,
        rate_type: typing.Optional[RateCompareRequestRateType] = OMIT,
        location: typing.Optional[RateCompareLocation] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> RateComparison:
        """
        Get summary statistics (min, max, average, quartiles) for prices matching the selected filters. When no Network ID is provided, cash prices are shown.

        Parameters
        ----------
        ssp_id : typing.Optional[str]
            Filter to a single SSP. Upstream-derived from the dataset and may change as the dataset is rebuilt. Do not bake into URLs, bookmarks, or persistent storage.

        provider_id : typing.Optional[str]
            Filter to a single provider. Upstream-derived from the dataset and may change as the dataset is rebuilt. Do not bake into URLs, bookmarks, or persistent storage.

        network_id : typing.Optional[str]
            Filter to a single insurance network. Int64String — JSON string-wrapped 64-bit integer, because JSON cannot safely represent values above 2^53. Upstream-derived from the dataset and may change as the dataset is rebuilt. Do not bake into URLs, bookmarks, or persistent storage.

        rate_type : typing.Optional[RateCompareRequestRateType]
            `cash` filters to rows with no network. `negotiated` requires a network match.

        location : typing.Optional[RateCompareLocation]
            Optional location scope. Exactly zero or one of `near`, `within`, or `zip_anchor` modes.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RateComparison
            Successful Response

        Examples
        --------
        import asyncio

        from turquoise-health import AsyncTurquoiseHealth, RateCompareLocation

        client = AsyncTurquoiseHealth(token="YOUR_TOKEN", base_url="https://yourhost.com/path/to/api", )
        async def main() -> None:
            await client.consumer_pricing.v2compare_prices(ssp_id='RA011', network_id='8361580493441765265', location=RateCompareLocation(within_state='CO', ), )
        asyncio.run(main())
        """
        _response = await self._raw_client.v2compare_prices(
            ssp_id=ssp_id,
            provider_id=provider_id,
            network_id=network_id,
            rate_type=rate_type,
            location=location,
            request_options=request_options,
        )
        return _response.data
